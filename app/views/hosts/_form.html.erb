<%= form_with(model: host, local: true) do |form| %>
  <% if host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(host.errors.count, "error") %> prohibited this host from being saved:</h2>

      <ul>
      <% host.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :host_name %>
  </div>

  <div class="field">
    <%= form.label :domain %>
    <%= form.text_field :domain, id: :host_domain %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :host_active %>
  </div>

  <div class="field">
    <%= form.label :tier %>
    <%= form.text_field :tier, id: :host_tier %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
